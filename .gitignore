# macOS adds a file when opening a directory with Finder
.DS_Store

# Running `nixos-rebuild build` or `nix build` generates a result directory
result

# Running `nixos-rebuild build-vm` and starting the VM adds a file for VM storage drive
*.qcow2

# Find a way to move these to a secrets
modules/nixos/suites/work/default.nix
